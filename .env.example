# WXNET Configuration

# Default location (will be auto-detected if not set)
DEFAULT_LATITUDE=35.0
DEFAULT_LONGITUDE=-97.5
DEFAULT_LOCATION=Oklahoma City, OK

# API Keys (optional - using free APIs as defaults)
OPENWEATHER_API_KEY=
NWS_USER_AGENT=WXNET Weather Terminal (contact@example.com)

# Update intervals (seconds)
WEATHER_UPDATE_INTERVAL=300
ALERT_UPDATE_INTERVAL=60
RADAR_UPDATE_INTERVAL=120

# Display settings
USE_COLOR=true
ALERT_SOUND=false
ANIMATION_SPEED=medium
